# Acrolinx SSO Proxy Server Configuration
# Copy this file to .env and fill in your actual values
# 
# [SECURITY] Never commit your .env file to version control!
# The .env file should be in .gitignore

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Acrolinx server URL (required)
ACROLINX_SERVER_URL=https://your-acrolinx-server.acrolinx.cloud

# SSO secret/generic password (required)
# Get this from your Acrolinx administrator
ACROLINX_SSO_PASSWORD=your-sso-secret-here

# =============================================================================
# OPTIONAL VARIABLES
# =============================================================================

# Default username for SSO authentication
ACROLINX_SSO_USERNAME=default-user

# Client signature (base64 client ID or JWT signature)
# This is used by the Acrolinx Sidebar to identify your integration
ACROLINX_CLIENT_SIGNATURE=SW50ZWdyYXRpb25EZXZlbG9wbWVudERlbW9Pbmx5

# Integration version string (appended to client signature)
# Format: "; YourApp v1.0.0"
ACROLINX_INTEGRATION_VERSION=; PoC v1.0.0

# Port for the proxy server (default: 3002)
# Note: Port 3001 is used by BrowserSync UI
SSO_PROXY_PORT=3002

# Comma-separated list of allowed CORS origins
# Add all domains that will call the proxy server
ALLOWED_ORIGINS=http://localhost:3000

# Logging level: DEBUG, INFO, WARN, ERROR (default: INFO)
LOG_LEVEL=INFO
